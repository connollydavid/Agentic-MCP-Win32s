# The Where room — the software under development, embedded as a bare store with
# worktrees (call/0004). Materialize with `host-lifecycle software --materialize .`;
# audit with `host-lifecycle software --check .`. The canonical worktree mcp-win32s/
# is created at `pin`; "bump the pointer" is now "update this pin".
[software "mcp-win32s"]
	url = git@github.com:connollydavid/MCP-Win32s.git
	pin = 792da6152d27d38c660ff30078e921e349c7f4a5
